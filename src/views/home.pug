extends layout.pug

block content
  section(class="section_form") 
    h1 Bienvenue sur notre blog
    ul
      if blogs
        each blog in blogs
          li
            a(href=`/blog?id=${blog.id}`)= blog.title
      else
        li Aucun blog disponible


        